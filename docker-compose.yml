version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
  ai-service:
    build: ./ai-service
    ports:
      - "8000:8000"
