spring:
  datasource:
    url: jdbc:postgresql://db.yfjzozcvawtmzmybhycx.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: Midhun@G2006
    driver-class-name: org.postgresql.Driver

  jackson:
    serialization:
      write-dates-as-timestamps: false

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

server:
  port: ${SERVER_PORT:8080}

careerguidance:
  app:
    jwtSecret: ${JWT_SECRET:careerguidancesecretkey2024supersecureandlongenoughforhs256algorithm}
    jwtExpirationMs: ${JWT_EXPIRATION_MS:86400000}