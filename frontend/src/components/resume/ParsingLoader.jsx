import { motion } from 'framer-motion';
import { CheckCircle2, Sparkles } from 'lucide-react';

export default function ParsingLoader({ progress }) {
  const steps = ['Uploading resume', 'Extracting skills', 'Reading education', 'Finding projects', 'Finding experience', 'Analyzing certifications', 'Generating summary', 'Finalizing'];
  const active = Math.min(steps.length - 1, Math.floor(progress / (100 / steps.length)));
  return <div className="mx-auto max-w-2xl rounded-2xl border border-blue-100 bg-white p-6 shadow-xl shadow-blue-100/40 sm:p-10"><div className="mx-auto flex h-20 w-20 items-center justify-center rounded-3xl bg-gradient-to-br from-blue-600 to-sky-500 text-white shadow-lg shadow-blue-200"><motion.div animate={{ rotate: 360 }} transition={{ duration: 3, repeat: Infinity, ease: 'linear' }}><Sparkles size={34} /></motion.div></div><h1 className="mt-6 text-center text-2xl font-black text-slate-950">AI is reading your resume</h1><p className="mt-2 text-center text-sm leading-6 text-slate-500">We are organizing your experience into a career-ready profile.</p><div className="mt-8 h-3 overflow-hidden rounded-full bg-slate-100"><motion.div className="h-full rounded-full bg-gradient-to-r from-blue-600 to-sky-500" animate={{ width: `${progress}%` }} /></div><div className="mt-2 flex justify-between text-xs font-bold text-slate-500"><span>{steps[active]}...</span><span>{progress}%</span></div><div className="mt-8 grid gap-3 sm:grid-cols-2">{steps.map((step, index) => <div key={step} className={`flex items-center gap-2.5 rounded-xl px-3 py-2.5 text-sm font-semibold ${index <= active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-50 text-slate-400'}`}><CheckCircle2 size={17} className={index <= active ? 'text-emerald-500' : 'text-slate-300'} />{step}</div>)}</div><p className="mt-7 text-center text-xs text-slate-400">Usually takes less than a minute. Please keep this page open.</p></div>;
}
