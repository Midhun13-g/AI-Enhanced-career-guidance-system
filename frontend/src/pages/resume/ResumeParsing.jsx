import { useEffect, useState } from 'react'; import { useNavigate } from 'react-router-dom'; import AppLayout from '../../components/layout/AppLayout'; import ParsingLoader from '../../components/resume/ParsingLoader';
export default function ResumeParsing() { const [progress, setProgress] = useState(6); const navigate = useNavigate(); useEffect(() => { const timer = setInterval(() => setProgress((value) => { if (value >= 100) { clearInterval(timer); setTimeout(() => navigate('/resume/editor'), 500); return 100; } return Math.min(100, value + 7); }), 430); return () => clearInterval(timer); }, [navigate]); return <AppLayout><div className="py-6"><ParsingLoader progress={progress} /></div></AppLayout>; }
