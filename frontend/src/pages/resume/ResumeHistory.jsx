import { useEffect, useState } from 'react'; import { Download, Eye, FileText, Plus, Trash2 } from 'lucide-react'; import { Link } from 'react-router-dom'; import AppLayout from '../../components/layout/AppLayout'; import { deleteResume, getResumeHistory } from '../../services/resumeService'; import { useToast } from '../../context/ToastContext';
const fallback = [{ id: 'demo-resume', fileName: 'Alex_Johnson_Resume.pdf', uploadDate: 'Today', score: 82, status: 'Analyzed' }, { id: 'previous', fileName: 'Alex_Johnson_Resume_v1.pdf', uploadDate: '12 Jul 2026', score: 74, status: 'Analyzed' }];
export default function ResumeHistory() { const [items, setItems] = useState(fallback); const toast = useToast(); useEffect(() => { getResumeHistory().then(({ data }) => Array.isArray(data) && setItems(data)).catch(() => {}); }, []); const remove = async (id) => { try { await deleteResume(id); } catch {} setItems((old) => old.filter((item) => item.id !== id)); toast('Resume removed from your history.', 'success'); }; return <AppLayout><div className="mx-auto max-w-5xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-wider text-blue-600">Module 4</p><h1 className="mt-2 text-3xl font-black text-slate-950">Resume history</h1><p className="mt-2 text-slate-600">Review previously uploaded resumes and their analysis results.</p></div><Link to="/resume/upload" className="inline-flex items-center gap-2 rounded-xl bg-blue-600 px-5 py-2.5 text-sm font-bold text-white hover:bg-blue-700"><Plus size={17} />Upload resume</Link></div><div className="mt-7 overflow-hidden rounded-2xl border border-slate-200 bg-white">{items.map((item) => <div key={item.id} className="flex flex-wrap items-center gap-4 border-b border-slate-100 p-5 last:border-0"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-red-50 text-red-500"><FileText size={21} /></div><div className="min-w-[180px] flex-1"><p className="font-bold text-slate-900">{item.fileName || item.name}</p><p className="mt-1 text-sm text-slate-500">Uploaded {item.uploadDate || item.createdAt}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-sm font-bold text-emerald-700">{item.score || item.resumeScore}% score</span><span className="rounded-full bg-blue-50 px-3 py-1 text-sm font-bold text-blue-700">{item.status || 'Analyzed'}</span><div className="flex gap-1"><Link to="/resume/analysis" aria-label="View analysis" className="rounded-lg p-2 text-slate-500 hover:bg-blue-50 hover:text-blue-600"><Eye size={18} /></Link><button aria-label="Download resume" className="rounded-lg p-2 text-slate-500 hover:bg-blue-50 hover:text-blue-600"><Download size={18} /></button><button onClick={() => remove(item.id)} aria-label="Delete resume" className="rounded-lg p-2 text-slate-500 hover:bg-red-50 hover:text-red-600"><Trash2 size={18} /></button></div></div>)}</div></div></AppLayout>; }
