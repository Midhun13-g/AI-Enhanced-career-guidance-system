import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useForm } from 'react-hook-form'; import { motion } from 'framer-motion'; import { CheckCircle2, ChevronDown, Edit3, Plus, Save, X } from 'lucide-react'; import AppLayout from '../../components/layout/AppLayout'; import { demoResume } from './resumeData'; import { updateResume } from '../../services/resumeService'; import { useToast } from '../../context/ToastContext';
const Field = ({ label, name, register }) => <label className="block"><span className="mb-1.5 block text-xs font-bold text-slate-600">{label}</span><input className="input-field py-2.5" {...register(name)} /></label>;
export default function ResumeEditor() { const navigate = useNavigate(); const toast = useToast(); const { register, handleSubmit, reset } = useForm({ defaultValues: demoResume }); const [skills, setSkills] = useState(demoResume.skills); const [newSkill, setNewSkill] = useState(''); const [saving, setSaving] = useState(false); const save = async (values) => { setSaving(true); const data = { ...values, skills }; try { await updateResume(sessionStorage.getItem('resumeId') || 'demo-resume', data); } catch { /* UI remains functional while API is unavailable. */ } finally { setSaving(false); toast('Extracted profile saved.', 'success'); navigate('/resume/analysis'); } }; const addSkill = () => { const trimmed = newSkill.trim(); if (trimmed && !skills.includes(trimmed)) setSkills([...skills, trimmed]); setNewSkill(''); };
 return <AppLayout><div className="mx-auto max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-wider text-blue-600">Module 4 · Step 3 of 4</p><h1 className="mt-2 text-3xl font-black text-slate-950">Review your extracted profile</h1><p className="mt-2 text-slate-600">AI found the information below. Edit anything before saving it.</p></div><div className="flex gap-3"><button onClick={() => reset(demoResume)} className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-bold text-slate-600 hover:bg-slate-50">Reset changes</button><button onClick={handleSubmit(save)} disabled={saving} className="inline-flex items-center gap-2 rounded-xl bg-blue-600 px-5 py-2.5 text-sm font-bold text-white hover:bg-blue-700"><Save size={17} />{saving ? 'Saving...' : 'Save profile'}</button></div></div><div className="mt-7 grid gap-6 lg:grid-cols-[1fr_300px]"><form onSubmit={handleSubmit(save)} className="space-y-5"><section className="card p-5 sm:p-6"><h2 className="flex items-center gap-2 text-lg font-black text-slate-900"><Edit3 size={19} className="text-blue-600" />Personal information</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Full name" name="name" register={register} /><Field label="Email" name="email" register={register} /><Field label="Phone" name="phone" register={register} /><Field label="Location" name="location" register={register} /><Field label="LinkedIn" name="linkedin" register={register} /><Field label="GitHub" name="github" register={register} /></div></section><section className="card p-5 sm:p-6"><h2 className="text-lg font-black text-slate-900">Skills</h2><div className="mt-4 flex flex-wrap gap-2">{skills.map((skill) => <motion.span layout key={skill} className="inline-flex items-center gap-1 rounded-full bg-blue-50 px-3 py-1.5 text-sm font-bold text-blue-700">{skill}<button type="button" onClick={() => setSkills(skills.filter((item) => item !== skill))} aria-label={`Remove ${skill}`}><X size={14} /></button></motion.span>)}</div><div className="mt-4 flex gap-2"><input value={newSkill} onChange={(event) => setNewSkill(event.target.value)} onKeyDown={(event) => event.key === 'Enter' && (event.preventDefault(), addSkill())} className="input-field py-2" placeholder="Add a skill" /><button type="button" onClick={addSkill} className="inline-flex shrink-0 items-center gap-1 rounded-xl bg-slate-900 px-4 py-2 text-sm font-bold text-white"><Plus size={16} />Add</button></div></section>{[['Education', demoResume.education, ['degree', 'school', 'year', 'cgpa']], ['Projects', demoResume.projects, ['name', 'description', 'technologies']], ['Experience', demoResume.experience, ['company', 'role', 'years', 'description']], ['Certifications', demoResume.certifications, ['title', 'provider', 'date']]].map(([heading, records, fields]) => <section key={heading} className="card p-5 sm:p-6"><div className="flex items-center justify-between"><h2 className="text-lg font-black text-slate-900">{heading}</h2><button type="button" className="inline-flex items-center gap-1 text-sm font-bold text-blue-600"><Plus size={16} />Add</button></div>{records.map((record, index) => <div key={index} className="mt-4 rounded-xl border border-slate-100 bg-slate-50 p-4"><div className="grid gap-3 sm:grid-cols-2">{fields.map((field) => <Field key={field} label={field[0].toUpperCase() + field.slice(1)} name={`${heading.toLowerCase()}.${index}.${field}`} register={register} />)}</div></div>)}</section>)}</form><aside className="space-y-5"><div className="card p-5"><div className="flex items-center justify-between"><p className="font-bold text-slate-900">Profile completion</p><span className="font-black text-emerald-600">88%</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full w-[88%] rounded-full bg-emerald-500" /></div><p className="mt-4 text-sm leading-6 text-slate-500">Add a portfolio link and a professional summary to make your profile complete.</p></div><div className="rounded-2xl bg-gradient-to-br from-blue-600 to-sky-500 p-5 text-white"><CheckCircle2 size={23} /><h3 className="mt-3 font-black">AI extraction complete</h3><p className="mt-2 text-sm leading-6 text-blue-100">We identified 8 skills, 2 projects, 1 education entry, and 1 certification.</p></div></aside></div></div></AppLayout>; }
