import { Box, Button, Card, CardContent, Chip, Typography } from '@mui/material'; import { useAuth } from '../context/AuthContext';
export default function PendingVerificationPage(){const {user}=useAuth();return <Box sx={{maxWidth:650,mx:'auto',p:4}}><Card><CardContent sx={{p:4}}><Chip label="PENDING VERIFICATION" color="warning"/><Typography variant="h4" fontWeight={800} mt={2}>Your mentor application is under review</Typography><Typography mt={2}>You cannot access mentor features until an administrator approves your account.</Typography><Typography color="text.secondary" mt={2}>Applicant: {user?.email || 'your account'}</Typography><Button href="mailto:support@careerai.com" variant="outlined" sx={{mt:4}}>Contact Support</Button></CardContent></Card></Box>}
