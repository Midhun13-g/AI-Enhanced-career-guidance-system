import { useEffect, useMemo, useState } from 'react';
import { motion } from 'framer-motion';
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { ArrowDownToLine, ChevronLeft, ChevronRight, Eye, FileText, MoreHorizontal, Plus, Search, Trash2, Users, X } from 'lucide-react';
import { adminService } from '../../services/adminService';

const sampleStudents = [{id:1,name:'Aarav Mehta',email:'aarav@college.edu',course:'B.Tech Computer Science',cgpa:8.7,skills:'React, Python',resume:'Uploaded',status:'Active'},{id:2,name:'Ananya Sharma',email:'ananya@college.edu',course:'BCA',cgpa:9.1,skills:'Java, SQL',resume:'Uploaded',status:'Active'},{id:3,name:'Rohan Gupta',email:'rohan@college.edu',course:'B.Tech Information Tech.',cgpa:7.9,skills:'Node.js, Figma',resume:'Pending',status:'Active'},{id:4,name:'Priya Nair',email:'priya@college.edu',course:'MCA',cgpa:8.4,skills:'Data Analysis',resume:'Uploaded',status:'Disabled'}];
const sampleQuestions = [{id:1,question:'Which data structure uses LIFO ordering?',category:'Technical Skills',type:'MCQ',status:'Enabled'},{id:2,question:'I enjoy solving complex problems.',category:'Personality',type:'Likert Scale',status:'Enabled'},{id:3,question:'Rate your interest in software development.',category:'Interest',type:'Rating',status:'Disabled'}];
const sampleResumes = [{id:1,name:'Aarav Mehta',file:'aarav_mehta_resume.pdf',uploaded:'Jul 28, 2026',score:88,ats:91,status:'Analyzed'},{id:2,name:'Ananya Sharma',file:'ananya_sharma.pdf',uploaded:'Jul 27, 2026',score:82,ats:86,status:'Analyzed'},{id:3,name:'Rohan Gupta',file:'rohan_resume.pdf',uploaded:'Jul 26, 2026',score:74,ats:78,status:'Review needed'}];
const registrations = [{m:'Jan',n:42},{m:'Feb',n:58},{m:'Mar',n:76},{m:'Apr',n:68},{m:'May',n:104},{m:'Jun',n:121},{m:'Jul',n:137}]; const interest = [{name:'Technology',value:38},{name:'Business',value:25},{name:'Design',value:19},{name:'Science',value:18}]; const palette=['#6366f1','#14b8a6','#f59e0b','#f43f5e'];
const Card = ({label,value,trend,icon:Icon,color='indigo'}) => <motion.div initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><div className="flex justify-between"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold tracking-tight">{value}</p>{trend && <p className="mt-2 text-xs font-semibold text-emerald-600">↑ {trend} from last month</p>}</div><div className={`grid h-11 w-11 place-items-center rounded-xl bg-${color}-50 text-${color}-600`}><Icon size={21}/></div></div></motion.div>;
const Empty = ({title}) => <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center"><p className="font-semibold text-slate-700">{title}</p><p className="mt-1 text-sm text-slate-500">This module is ready to connect to its administration endpoint.</p></div>;
function Toolbar({value,setValue,action}) { return <div className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="relative max-w-md flex-1"><Search className="absolute left-3 top-3 text-slate-400" size={18}/><input className="w-full rounded-xl border border-slate-200 bg-white py-2.5 pl-10 pr-3 text-sm outline-none focus:border-indigo-400" placeholder="Search records..." value={value} onChange={e=>setValue(e.target.value)}/></div>{action}</div> }
function Confirm({item,onClose,onConfirm}) { if(!item) return null; return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/45 p-4"><div className="w-full max-w-sm rounded-2xl bg-white p-6 shadow-2xl"><h3 className="text-lg font-bold">Delete record?</h3><p className="mt-2 text-sm text-slate-500">This will permanently remove {item.name || 'this item'}. This action cannot be undone.</p><div className="mt-6 flex justify-end gap-3"><button onClick={onClose} className="rounded-lg px-4 py-2 text-sm font-semibold">Cancel</button><button onClick={onConfirm} className="rounded-lg bg-rose-600 px-4 py-2 text-sm font-semibold text-white">Delete</button></div></div></div> }
export function AdminDashboard() { const [data,setData]=useState(null); useEffect(()=>{adminService.getDashboard().then(setData).catch(()=>{});},[]); const metrics=data?.metrics || data || {}; return <><div className="mb-8"><p className="text-sm font-semibold text-indigo-600">OVERVIEW</p><h1 className="mt-1 text-3xl font-bold">Welcome back, Admin</h1><p className="mt-2 text-slate-500">Here’s how the career guidance platform is performing today.</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card label="Total Students" value={metrics.totalStudents ?? '2,486'} trend="12.5%" icon={Users}/><Card label="Uploaded Resumes" value={metrics.uploadedResumes ?? '1,874'} trend="8.2%" icon={FileText}/><Card label="Completed Assessments" value={metrics.completedAssessments ?? '1,628'} trend="16.4%" icon={Eye}/><Card label="Average ATS Score" value={`${metrics.averageAtsScore ?? 78}%`} trend="3.1%" icon={ArrowDownToLine}/></div><div className="mt-7 grid gap-6 xl:grid-cols-3"><section className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm xl:col-span-2"><div><h2 className="font-bold">User registrations</h2><p className="text-sm text-slate-500">New students joining the platform</p></div><div className="mt-5 h-72"><ResponsiveContainer><AreaChart data={registrations}><defs><linearGradient id="fill" x1="0" y1="0" x2="0" y2="1"><stop stopColor="#6366f1" stopOpacity=".35"/><stop offset="1" stopColor="#6366f1" stopOpacity="0"/></linearGradient></defs><CartesianGrid vertical={false} stroke="#eef2f7"/><XAxis dataKey="m" tickLine={false} axisLine={false}/><YAxis tickLine={false} axisLine={false}/><Tooltip/><Area dataKey="n" stroke="#6366f1" strokeWidth={3} fill="url(#fill)"/></AreaChart></ResponsiveContainer></div></section><section className="rounded-2xl border border-slate-100 bg-white p-6 shadow-sm"><h2 className="font-bold">Career interests</h2><p className="text-sm text-slate-500">Top student preferences</p><div className="mt-2 h-60"><ResponsiveContainer><PieChart><Pie data={interest} dataKey="value" innerRadius={52} outerRadius={82} paddingAngle={4}>{interest.map((_,i)=><Cell key={i} fill={palette[i]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div><div className="grid grid-cols-2 gap-2 text-xs">{interest.map((x,i)=><span key={x.name} className="flex items-center gap-1"><i className="h-2 w-2 rounded-full" style={{background:palette[i]}}/>{x.name}</span>)}</div></section></div><section className="mt-7 rounded-2xl border border-slate-100 bg-white p-6 shadow-sm"><h2 className="font-bold">Assessment completion</h2><div className="mt-5 h-56"><ResponsiveContainer><BarChart data={registrations}><XAxis dataKey="m"/><YAxis/><Tooltip/><Bar dataKey="n" fill="#14b8a6" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></section></> }
export function StudentsPage() { const [records,setRecords]=useState([]), [query,setQuery]=useState(''), [del,setDel]=useState(null), [loading,setLoading]=useState(true), [error,setError]=useState(''); useEffect(()=>{setLoading(true);adminService.getStudents({size:100}).then(x=>setRecords(x.content || x || [])).catch(()=>setError('Unable to load student profiles from the server.')).finally(()=>setLoading(false));},[]); const shown=useMemo(()=>records.filter(x=>JSON.stringify(x).toLowerCase().includes(query.toLowerCase())),[records,query]); const remove=async()=>{try{await adminService.deleteStudent(del.id);setRecords(r=>r.filter(x=>x.id!==del.id));}catch{setError('Unable to delete this student.');}setDel(null)}; return <><header className="mb-7"><p className="text-sm font-semibold text-indigo-600">STUDENT PROFILES</p><h1 className="mt-1 text-3xl font-bold">Student profiles</h1><p className="mt-2 text-slate-500">Search, review and manage registered student information.</p></header>{error&&<p className="mb-4 rounded-xl bg-rose-50 px-4 py-3 text-sm text-rose-700">{error}</p>}<section className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><Toolbar value={query} setValue={setQuery}/>{loading?<div className="p-10 text-center text-sm text-slate-500">Loading student profiles...</div>:shown.length===0?<div className="p-10 text-center text-sm text-slate-500">No student profiles found.</div>:<><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-y border-slate-100 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{['Student','College','CGPA','Status',''].map(x=><th className="px-4 py-3 font-semibold" key={x}>{x}</th>)}</tr></thead><tbody>{shown.map(x=><tr key={x.id} className="border-b border-slate-50 hover:bg-slate-50/80"><td className="px-4 py-4"><p className="font-semibold">{x.firstName} {x.lastName}</p><p className="text-xs text-slate-500">{x.email}</p></td><td className="px-4 py-4 text-slate-600">{x.collegeName || '—'}</td><td className="px-4 py-4 font-semibold">{x.cgpa ?? '—'}</td><td className="px-4 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${x.active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-600'}`}>{x.active?'Active':'Disabled'}</span></td><td className="px-4 py-4"><button onClick={()=>setDel({...x,name:`${x.firstName} ${x.lastName}`})} className="text-rose-500" aria-label="Delete student"><Trash2 size={17}/></button></td></tr>)}</tbody></table></div><Pagination count={shown.length}/></>}</section><Confirm item={del} onClose={()=>setDel(null)} onConfirm={remove}/></> }
const Pagination=({count})=><div className="flex items-center justify-between pt-5 text-sm text-slate-500"><span>Showing {count} records</span><div className="flex gap-1"><button className="rounded-lg border p-2"><ChevronLeft size={15}/></button><button className="rounded-lg bg-indigo-600 px-3 text-white">1</button><button className="rounded-lg border p-2"><ChevronRight size={15}/></button></div></div>;
export function QuestionsPage() { const [rows,setRows]=useState(sampleQuestions),[query,setQuery]=useState(''),[del,setDel]=useState(null); const [form,setForm]=useState(false); useEffect(()=>{adminService.getQuestions().then(x=>setRows(x.content||x)).catch(()=>{});},[]); const shown=rows.filter(x=>JSON.stringify(x).toLowerCase().includes(query.toLowerCase())); const remove=async()=>{try{await adminService.deleteQuestion(del.id)}catch{}setRows(x=>x.filter(q=>q.id!==del.id));setDel(null)}; return <><header className="mb-7"><p className="text-sm font-semibold text-indigo-600">ASSESSMENT MANAGEMENT</p><h1 className="mt-1 text-3xl font-bold">Question bank</h1><p className="mt-2 text-slate-500">Create and maintain evaluation questions across all categories.</p></header><section className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><Toolbar value={query} setValue={setQuery} action={<button onClick={()=>setForm(true)} className="btn-primary flex gap-2"><Plus size={17}/>Create question</button>}/><div className="space-y-3">{shown.map(q=><div className="flex items-center gap-4 rounded-xl border border-slate-100 p-4" key={q.id}><div className="grid h-9 w-9 place-items-center rounded-lg bg-indigo-50 font-bold text-indigo-600">{q.id}</div><div className="min-w-0 flex-1"><p className="font-semibold">{q.question}</p><p className="mt-1 text-xs text-slate-500">{q.category} · {q.type}</p></div><span className={`rounded-full px-2.5 py-1 text-xs ${q.status==='Enabled'?'bg-emerald-50 text-emerald-700':'bg-slate-100'}`}>{q.status}</span><button onClick={()=>setDel(q)} className="text-rose-500"><Trash2 size={17}/></button></div>)}</div></section>{form&&<QuestionModal onClose={()=>setForm(false)} onSave={async data=>{try{const v=await adminService.createQuestion(data);setRows(r=>[v,...r])}catch{setRows(r=>[{...data,id:Date.now(),status:'Enabled'},...r])}setForm(false)}}/>}<Confirm item={del} onClose={()=>setDel(null)} onConfirm={remove}/></> }
function QuestionModal({onClose,onSave}) { const [question,setQuestion]=useState(''); const [category,setCategory]=useState('Technical Skills'); return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/45 p-4"><form onSubmit={e=>{e.preventDefault();onSave({question,category,type:'MCQ',options:[]})}} className="w-full max-w-lg rounded-2xl bg-white p-6"><div className="flex justify-between"><h2 className="text-lg font-bold">Create question</h2><button type="button" onClick={onClose}><X/></button></div><label className="mt-5 block text-sm font-semibold">Question<textarea required value={question} onChange={e=>setQuestion(e.target.value)} className="mt-2 w-full rounded-xl border p-3 font-normal" rows="3"/></label><label className="mt-4 block text-sm font-semibold">Category<select value={category} onChange={e=>setCategory(e.target.value)} className="mt-2 w-full rounded-xl border p-3 font-normal">{['Technical Skills','Aptitude','Personality','Interest'].map(x=><option key={x}>{x}</option>)}</select></label><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onClose}>Cancel</button><button className="btn-primary">Create question</button></div></form></div> }
export function ResumesPage() { const [rows,setRows]=useState(sampleResumes),[query,setQuery]=useState(''),[del,setDel]=useState(null);useEffect(()=>{adminService.getResumes().then(x=>setRows(x.content||x)).catch(()=>{});},[]);const shown=rows.filter(x=>JSON.stringify(x).toLowerCase().includes(query.toLowerCase()));const remove=async()=>{try{await adminService.deleteResume(del.id)}catch{}setRows(x=>x.filter(r=>r.id!==del.id));setDel(null)};return <><header className="mb-7"><p className="text-sm font-semibold text-indigo-600">RESUME MANAGEMENT</p><h1 className="mt-1 text-3xl font-bold">Resume library</h1><p className="mt-2 text-slate-500">Review uploaded resumes, analysis and ATS performance.</p></header><section className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><Toolbar value={query} setValue={setQuery}/><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-y bg-slate-50 text-xs uppercase text-slate-500"><tr>{['Student','File','Uploaded','Resume score','ATS score','Status',''].map(x=><th className="px-4 py-3" key={x}>{x}</th>)}</tr></thead><tbody>{shown.map(x=><tr className="border-b border-slate-50" key={x.id}><td className="px-4 py-4 font-semibold">{x.name}</td><td className="px-4 py-4 text-slate-500">{x.file}</td><td className="px-4 py-4">{x.uploaded}</td><td className="px-4 py-4 font-bold text-indigo-600">{x.score}%</td><td className="px-4 py-4 font-bold text-teal-600">{x.ats}%</td><td className="px-4 py-4"><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs text-emerald-700">{x.status}</span></td><td className="px-4 py-4"><div className="flex gap-2"><button onClick={()=>adminService.downloadResume(x.id).catch(()=>{})} className="text-indigo-600"><ArrowDownToLine size={17}/></button><button onClick={()=>setDel(x)} className="text-rose-500"><Trash2 size={17}/></button></div></td></tr>)}</tbody></table></div></section><Confirm item={del} onClose={()=>setDel(null)} onConfirm={remove}/></> }
export function PlaceholderPage({title,description}) { return <><header className="mb-7"><p className="text-sm font-semibold text-indigo-600">ADMINISTRATION</p><h1 className="mt-1 text-3xl font-bold">{title}</h1><p className="mt-2 text-slate-500">{description}</p></header><Empty title={`${title} workspace`} /></> }
