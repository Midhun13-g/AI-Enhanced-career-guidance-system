import { useEffect, useMemo, useState } from 'react';
import { Alert, Button, Chip, Dialog, DialogActions, DialogContent, DialogTitle, MenuItem, Select, Skeleton, TextField } from '@mui/material';
import { mentorVerificationService as service } from '../../services/mentorVerificationService';
const statuses=['PENDING','UNDER_REVIEW','APPROVED','REJECTED','SUSPENDED'];
const badge={PENDING:'warning',UNDER_REVIEW:'info',APPROVED:'success',REJECTED:'error',SUSPENDED:'default'};
export default function MentorVerificationPage(){const [rows,setRows]=useState([]),[query,setQuery]=useState(''),[status,setStatus]=useState(''),[selected,setSelected]=useState(null),[dialog,setDialog]=useState(null),[reason,setReason]=useState(''),[loading,setLoading]=useState(true);const load=()=>{setLoading(true);service.list({status:status||undefined}).then(x=>setRows(x.content||x||[])).catch(()=>setRows([])).finally(()=>setLoading(false))};useEffect(load,[status]);const filtered=useMemo(()=>rows.filter(x=>JSON.stringify(x).toLowerCase().includes(query.toLowerCase())),[rows,query]);const act=async()=>{if((dialog==='reject'||dialog==='request-info'||dialog==='suspend')&&!reason.trim())return;try{if(dialog==='approve')await service.approve(selected.id);if(dialog==='reject')await service.reject(selected.id,reason);if(dialog==='request-info')await service.requestInfo(selected.id,reason);if(dialog==='suspend')await service.suspend(selected.id,reason);load()}finally{setDialog(null);setReason('')}};return <div><header className="mb-7"><p className="text-sm font-semibold text-indigo-600">MENTOR VERIFICATION</p><h1 className="mt-1 text-3xl font-bold">Review mentor applications</h1><p className="mt-2 text-slate-500">Verify professional credentials and keep the mentor network trusted.</p></header><section className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><div className="mb-5 flex flex-col gap-3 sm:flex-row"><TextField size="small" label="Search mentors" value={query} onChange={e=>setQuery(e.target.value)} fullWidth/><Select size="small" value={status} onChange={e=>setStatus(e.target.value)} displayEmpty sx={{minWidth:180}}><MenuItem value="">All statuses</MenuItem>{statuses.map(x=><MenuItem key={x} value={x}>{x.replace('_',' ')}</MenuItem>)}</Select></div>{loading?<Skeleton variant="rectangular" height={260}/>:filtered.length===0?<Alert severity="info">No mentor applications found.</Alert>:<div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="border-y bg-slate-50 text-xs uppercase text-slate-500"><tr>{['Profile','Company','Experience','Documents','Status','Actions'].map(x=><th className="px-4 py-3" key={x}>{x}</th>)}</tr></thead><tbody>{filtered.map(x=><tr className="border-b border-slate-50" key={x.id}><td className="px-4 py-4"><p className="font-semibold">{x.fullName||x.name}</p><p className="text-xs text-slate-500">{x.email}</p></td><td className="px-4 py-4">{x.company||x.currentCompany||'—'}</td><td className="px-4 py-4">{x.experience??x.yearsOfExperience??'—'} years</td><td className="px-4 py-4">{x.documents?.length??'—'}</td><td className="px-4 py-4"><Chip size="small" color={badge[x.status]||'default'} label={(x.status||'PENDING').replace('_',' ')}/></td><td className="px-4 py-4"><div className="flex flex-wrap gap-2"><Button size="small" onClick={()=>service.detail(x.id).then(setSelected)}>View</Button>{x.status!=='APPROVED'&&<Button size="small" color="success" onClick={()=>{setSelected(x);setDialog('approve')}}>Approve</Button>}{x.status!=='REJECTED'&&<Button size="small" color="error" onClick={()=>{setSelected(x);setDialog('reject')}}>Reject</Button>}<Button size="small" onClick={()=>{setSelected(x);setDialog('request-info')}}>Request info</Button>{x.status!=='SUSPENDED'&&<Button size="small" color="warning" onClick={()=>{setSelected(x);setDialog('suspend')}}>Suspend</Button>}</div></td></tr>)}</tbody></table></div>}</section><Dialog open={!!selected&&!dialog} onClose={()=>setSelected(null)} fullWidth maxWidth="md"><DialogTitle>Mentor profile</DialogTitle><DialogContent dividers>{selected&&<div className="grid gap-3 sm:grid-cols-2">{Object.entries(selected).filter(([k])=>!['documents'].includes(k)).map(([k,v])=><div key={k}><p className="text-xs uppercase text-slate-500">{k}</p><p>{typeof v==='object'?JSON.stringify(v):String(v??'—')}</p></div>)}</div>}</DialogContent><DialogActions><Button onClick={()=>setSelected(null)}>Close</Button></DialogActions></Dialog><Dialog open={!!dialog} onClose={()=>setDialog(null)}><DialogTitle>{dialog==='approve'?'Approve mentor':dialog==='reject'?'Reject mentor':dialog==='suspend'?'Suspend mentor':'Request more information'}</DialogTitle><DialogContent>{dialog!=='approve'&&<TextField autoFocus multiline minRows={3} fullWidth required label="Reason" value={reason} onChange={e=>setReason(e.target.value)} sx={{mt:1}}/>}</DialogContent><DialogActions><Button onClick={()=>setDialog(null)}>Cancel</Button><Button variant="contained" onClick={act}>Confirm</Button></DialogActions></Dialog></div>}
